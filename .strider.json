{
  "plugins": [{
    "id": "custom",
    "enabled": true,
    "config": {
      "prepare": "echo 'hello'",
      "cleanup": "echo 'cleanup'"
    }
  }, {
    "id": "nonexistent",
    "enabled": true,
    "config": {
      "test": "make test"
    }
  }, {
    "id": "node",
    "enabled": true,
    "config": {
      "test": "make test"
    }
  }],
  "runner": {
    "id": "simple-runner"
  }
}
